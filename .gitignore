node_modules
.next
.open-next
next-env.d.ts
*.tsbuildinfo
.DS_Store
.next-run

# Dev server output, when it is started detached
.dev-server.log

# Local environment
.env*.local
.env

# Build output and local service state
out
.wrangler

# This is a pnpm repo (packageManager: pnpm@11.3.0). An npm run here leaves a
# second lockfile that silently drifts from pnpm-lock.yaml.
package-lock.json
